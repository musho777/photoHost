import {
  SafeAreaView,
} from 'react-native';
import { SearchBlock } from './SearchBlock';


export const SearchScreen = ({ navigation }) => {
  return (
    <SafeAreaView >
      <SearchBlock />
    </SafeAreaView>
  );
};
